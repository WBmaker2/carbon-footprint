# 에코 체크: 우리 교실 탄소 발자국

## 1. MVP 목표
- 초등 4~6학년 학생이 교실에서 발생하는 쓰레기와 전기 사용량을 쉽게 입력할 수 있다.
- 선택한 날짜 기록을 열어도 입력/수정 즉시 대시보드와 차트에 반영된다.
- 최근 7일 활동 요약을 확인하고, 학생이 직접 줄일 수 있는 항목을 파악할 수 있다.
- 브라우저 `localStorage`에 저장된 값을 새로고침 후에도 유지한다.
- 차트는 Chart.js 기준으로 실시간 렌더링한다.

## 2. MVP 범위
### 포함
- 날짜 선택(오늘 + 과거 날짜 이동)
- 날짜별 기록 조회 및 히스토리 목록 표시
- 날짜별 기록 수정/재입력
- 최근 7일 요약 계산/표시
- 쓰레기 종류별 수량 입력
- 전기 사용 시간 입력
- 총합 대시보드
- 항목별 막대 그래프
- 간단한 탄소 환산 결과
- 전체 초기화
- 새로고침 후 데이터 유지(localStorage)

### 제외
- 로그인
- 서버/DB 저장
- 학급별 비교
- 교사용 관리자 페이지
- 학교 단위 다중 사용자 계정

## 3. 사용자 시나리오
1. 학생이 웹 앱을 연다.
2. 오늘 날짜가 기본 선택된 상태에서 버린 쓰레기와 전기 사용량을 항목별로 입력한다.
3. 입력 직후 대시보드 수치와 차트가 즉시 바뀐다.
4. 수업 흐름에 맞춰 과거 날짜를 선택해 기록을 다시 수정한다.
5. 최근 7일 추이와 히스토리 목록에서 날짜별 상태를 확인한다.
6. 선택 날짜의 기록을 정리하거나, 전체 초기화 버튼으로 브라우저 기록을 초기화한다.
7. 저장 오류가 생겨도 화면 상태는 유지되고 안내 메시지를 확인한다.

## 4. MVP 화면 구조
### 전체 레이아웃
- 상단 헤더
- 메인 2단 영역
- 하단 안내 영역

### 화면 와이어프레임
```text
+------------------------------------------------------+
| 에코 체크: 우리 교실 탄소 발자국                      |
| 날짜 선택 / 학습 목표                                 |
+---------------------------+--------------------------+
| 입력 패널                 | 선택 날짜 대시보드       |
|                           |                          |
| [플라스틱] [-]  3  [+]    | 총 입력 수량: 18         |
| [종이]     [-]  5  [+]    | 예상 탄소 배출: 2.4kg    |
| [캔]       [-]  1  [+]    | 가장 많은 항목: 종이     |
| [일반]     [-]  4  [+]    | 에코 상태: 보통          |
| [기본조명] [-] 360 [+]분  |                         |
| [HVAC]    [-] 30 [+]분     |                         |
|                           |                          |
| [전체 초기화]             |                          |
+------------------------------------------------------+
| 항목별 그래프 (Chart.js 막대 그래프)                 |
+------------------------------------------------------+
| 최근 7일 요약 / 선택 날짜 히스토리                     |
+------------------------------------------------------+
| 오늘의 한마디 / 실천 팁                               |
+------------------------------------------------------+
```

## 5. 화면 섹션 상세
### 5.1 헤더
- 앱 제목
- 날짜 선택(오늘/과거)
- 수업 연계 문구
  - 예: "환경을 지키는 생활 습관을 데이터로 확인해요"

### 5.2 입력 패널
- 항목 목록
  - 플라스틱
  - 종이
  - 캔
  - 일반쓰레기
  - 기본 조명(분, 기본값 360, 기본값은 기본 배출 계산에 반영)
  - 에어컨/온풍기(분, 학생 행동 항목)
- 입력 방식
  - `-` 버튼
  - 현재 값 표시
  - `+` 버튼
- 보조 규칙
  - 0 아래로 내려가지 않음
  - 기본 조명/에어컨은 30분 단위 증감
  - 기본 조명은 기본값(360분) 기준을 유지하고, 학생 조정은 에어컨/온풍기와 쓰레기 항목에서만 실천 항목으로 계산

### 5.3 대시보드 카드
- 총 입력 수량
- 예상 탄소 배출량
- 가장 많이 나온 항목
- 에코 상태
  - 좋음
  - 보통
  - 주의
- 차트 상태 메시지
  - 최근 선택 날짜/7일 요약의 차트 반영 상태
  - Chart.js 로딩 실패시 안내 메시지 표시

### 5.4 그래프 영역
- Chart.js 막대 그래프 + 최근 7일 추이 차트
- 일간 항목별 그래프는 막대형(쓰기 항목과 조절 항목)
- 주간 요약은 날짜별 비교용 라인형
- X축: 항목 이름 / 최근 날짜
- Y축: 입력값 또는 탄소 환산량
- 색상은 항목별 고정

### 5.5 하단 안내 영역
- 오늘의 실천 팁 1개 표시
- 예: "종이를 아껴 쓰면 탄소 배출을 줄일 수 있어요"

### 5.6 히스토리 패널
- 과거 날짜 선택 목록 표시
- 선택한 날짜 기록 수정 가능
- 과거 기록이 비어 있을 경우 `기록 없음`으로 안내

## 6. 데이터 구조안
```js
const state = {
  plastic: 0,
  paper: 0,
  can: 0,
  general: 0,
  baseLightingMinutes: 360,
  hvacMinutes: 0
};
```

### 메타 설정 예시
```js
const ITEMS = {
  plastic: { label: "플라스틱", color: "#ff8a65", carbonFactor: 0.12 },
  paper: { label: "종이", color: "#4db6ac", carbonFactor: 0.05 },
  can: { label: "캔", color: "#7986cb", carbonFactor: 0.09 },
  general: { label: "일반쓰레기", color: "#90a4ae", carbonFactor: 0.1 },
  baseLightingMinutes: {
    label: "기본 조명",
    color: "#ffd54f",
    carbonFactor: 0.0008,
    defaultValue: 360,
    affectsEcoStatus: false,
  },
  hvacMinutes: {
    label: "에어컨/온풍기",
    color: "#f08c3a",
    carbonFactor: 0.004,
    defaultValue: 0,
    affectsEcoStatus: true,
  },
};
```

### 현재 구현 메모
MVP 구현은 초안의 `electricityMinutes` 대신 아래 두 항목으로 전기 사용을 분리했다.

- `baseLightingMinutes`: 기본 조명 사용 시간. 기본값은 360분이며 전체 예상 탄소에는 포함하지만, 학생이 줄일 수 있는 실천 수량과 에코 상태 계산에서는 제외한다.
- `hvacMinutes`: 에어컨/온풍기 사용 시간. 30분 단위로 증감하며 학생 실천 수량, 주요 실천 대상, 에코 상태 계산에 포함한다.

기존 `electricityMinutes` 또는 `extraElectricityMinutes` 저장값은 `storage.js`에서 현재 모델로 변환한다.

## 7. 핵심 기능 구조
### 입력 처리
- 버튼 클릭 시 항목 값 증감
- 변경 후 상태 저장
- 화면과 그래프 즉시 갱신

### 계산 처리
- 총 입력 수량 계산
- 탄소 총합 계산
- 최다 항목 계산
- 상태 메시지 계산
- 최근 7일 요약 계산
- 선택 날짜별 기록 상태 계산

### 저장 처리
- 상태를 localStorage에 저장
- 앱 시작 시 저장된 값 복원
- 기록 저장 실패 시 화면 상태 유지 + 사용자 안내
- 저장 동작은 `{ ok, records, error }` 계약으로 반환되어 앱 레이어에서 성공/실패를 구분
- 전체 기록 JSON 내보내기(백업 파일)
- JSON 백업 가져오기
  - 병합(기본) 또는 교체 모드 선택 가능
- 최근 7일 요약 CSV 다운로드

### Phase 3 반영사항

- 저장/복원 실패가 화면 안내 메시지로 구분되어 표시된다.
- `readRawStorage()` 파싱 실패는 조용히 무시하지 않고 사용자에게 복구 상태를 알린다.
- 레거시 샘플 자동 제거는 실제 사용자 기록을 잘못 삭제할 위험을 낮추기 위해 중단했다.
- 저장 데이터는 날짜 키와 항목 값을 보수적으로 sanitize하고, 레거시 전기 필드(`electricityMinutes`, `extraElectricityMinutes`)만 현재 모델로 마이그레이션한다.

### Phase 4 반영사항

- 탄소 계산, 학생 실천 수량, 에코 상태, 선택 날짜/최근 7일 요약 view model 생성을 `calculations.js`로 분리했다.
- `app.js`는 DOM 렌더링, 이벤트 바인딩, 저장/백업 흐름 오케스트레이션에 집중한다.
- 날짜 키 생성/파싱/검증은 `date-utils.js`를 기준으로 사용한다.
- ES module 전환은 정적 앱 호환성과 변경 규모를 고려해 보류하고, 전역 스크립트 로드 순서를 문서로 관리한다.
- Chart.js는 이번 Phase에서 CDN 유지로 결정했다. 학교망에서 CDN이 막히면 차트 대신 안내 문구를 보여주는 fallback을 유지하고, 오프라인 정식 지원은 별도 vendor 전환 Phase에서 다룬다.

## 8. 파일 구성안
```text
carbon-footprint/
├─ index.html
├─ styles.css
├─ app.js
├─ data.js
├─ calculations.js
├─ chart.js
├─ date-utils.js
├─ storage.js
└─ assets/
   └─ icons/
```

## 9. 파일별 역할
### `index.html`
- 전체 레이아웃 마크업
- 헤더, 입력 패널, 대시보드, 차트 캔버스 배치
- Chart.js CDN 연결

### `styles.css`
- 전체 레이아웃
- 카드 스타일
- 버튼 스타일
- 초등학생 친화형 큰 버튼과 높은 가독성 색상 적용
- 반응형 처리

### `app.js`
- 앱 시작점
- 상태 초기화
- 이벤트 바인딩
- 전체 렌더 흐름 연결
- 계산된 view model을 받아 DOM과 차트를 갱신

### `data.js`
- 항목 메타 정보
- 탄소 계수
- 상태 메시지 기준값

### `calculations.js`
- 탄소 총합과 학생 실천 탄소 계산
- 학생 실천 수량, 주요 실천 대상, 에코 상태 계산
- 선택 날짜 요약 카드와 최근 7일 히스토리/추이 view model 생성

### `date-utils.js`
- 로컬 날짜 키(`YYYY-MM-DD`) 생성
- 날짜 키 파싱/검증
- 오늘 날짜 키 생성

### `chart.js`
- Chart.js 인스턴스 생성(일간 막대차트, 주간 추이차트)
- 차트 데이터 갱신 및 fallback 노출 함수를 함께 제공

### `storage.js`
- localStorage 저장/불러오기 함수 분리
- legacy `electricityMinutes` 마이그레이션 로직 포함

### `assets/icons/`
- 쓰레기/전기 아이콘 저장
- MVP에서는 없어도 되지만 확장 대비용 디렉터리

## 10. 구현 우선순위
1. `index.html`로 화면 뼈대 작성
2. `styles.css`로 기본 레이아웃 완성
3. `data.js`에 항목 정의
4. `app.js`에 상태 관리와 계산 로직 구현
5. `chart.js`에 막대 그래프 연결
6. `storage.js`로 localStorage 저장 추가
7. 문구와 색상 다듬기

## 11. MVP 완료 기준
- 모든 항목이 버튼으로 입력된다.
- 입력 직후 대시보드 수치가 바뀐다.
- 날짜를 바꿔 과거 기록을 수정해도 즉시 반영된다.
- 최근 7일 요약이 계산되어 표시된다.
- 그래프가 실시간으로 갱신된다.
- 탄소 총합이 계산되어 표시된다.
- 새로고침 후에도 값이 유지된다.
- 초기화 버튼이 전체 상태를 정상적으로 리셋한다.
- localStorage 저장 실패 안내가 별도 메시지로 노출된다.

## 12. Phase 2 완료 항목 반영

- `carbon-footprint-backup-YYYY-MM-DD.json` 형식으로 전체 기록을 내보낼 수 있다.
- JSON 백업을 병합 또는 교체 방식으로 가져와 날짜별 기록을 복원한다.
- 잘못된 JSON/형식 오류/파손된 파일은 기존 데이터를 유지하고, 상태 메시지로 실패를 안내한다.
- 최근 7일 요약 CSV가 `carbon-footprint-7day-summary-YYYY-MM-DD.csv` 파일명으로 다운로드된다.

## 13. 다음 구현 단계 제안
- 1차 개선: 백업/내보내기 및 복원 기능 설계(Phase 2)
- 2차 개선: 저장 실패/경합 케이스 안정화 및 복원 전략
- 3차 개선: 다중 클래스/반 운영용 보고서 산출
